CREATE TABLE employee_table(  
    age int NOT NULL,  
    name varchar(45) NOT NULL,  
    PRIMARY KEY (age)  
);  


CREATE TABLE `test` (
`Id` int NOT NULL,
`Name` char(64) NOT NULL,
PRIMARY KEY (`age`)
);